Added ready-to-use fairness constraint builders — `demographic_parity`, `equal_opportunity`, and `equalized_odds` (in `fair_seldonian.constraints.fairness`, re-exported at the top level). Each returns the postfix constraint string for a given tolerance `epsilon` and pair of groups, so common fairness definitions can be passed straight to `SeldonianConfig` without hand-writing the notation.
